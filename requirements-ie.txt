# Shared dependencies for the isolated Python 3.10 OCR and layout environments.
# Framework-specific packages belong in requirements-ocr.txt and
# requirements-layout.txt because Paddle and CUDA PyTorch bundle incompatible
# cuDNN DLLs on Windows.
jsonschema>=4.22
python-dateutil>=2.9
numpy>=1.26,<2.3
pandas>=2.0
Pillow>=10.0
PyMuPDF>=1.23
PyYAML>=6.0
opencv-python-headless==4.10.0.84
# Maintenance tools retain the original scikit-learn artifacts. Portable
# inference uses hash-bound numeric parameters instead of loading those pickles,
# so the verified Python 3.10 runtime can remain on its supported 1.7.x line.
scikit-learn==1.7.2; python_version < "3.11"
scikit-learn==1.8.0; python_version >= "3.11"
joblib>=1.3
pytest>=7.0
