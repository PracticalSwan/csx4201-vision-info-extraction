# Shared dependencies for the isolated Python 3.10 OCR and layout environments.
# Framework-specific packages belong in requirements-ocr.txt and
# requirements-layout.txt because Paddle and CUDA PyTorch bundle incompatible
# cuDNN DLLs on Windows.
jsonschema>=4.22
python-dateutil>=2.9
numpy>=1.26,<2.3
pandas>=2.0
Pillow>=10.0
PyMuPDF>=1.23
PyYAML>=6.0
opencv-python-headless==4.10.0.84
# The preserved display-only K-Means artifacts were serialized with 1.8.0.
# Python 3.10 is required by the verified OCR/layout runtimes and supports
# scikit-learn only through 1.7.x, so keep the documented compatibility split.
scikit-learn==1.7.2; python_version < "3.11"
scikit-learn==1.8.0; python_version >= "3.11"
joblib>=1.3
pytest>=7.0
