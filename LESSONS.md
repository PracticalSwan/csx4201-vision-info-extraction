# LESSONS.md — Mistakes and lessons log

> **READ RULE (MUST):** Read this at the start of every session. Do not repeat a mistake that is already recorded here.
> **How to use:** When a mistake happens or a non-obvious lesson is learned, add a row. Be specific about cause and the durable corrective action — not blame.
> Keep entries concise and chronological (newest at the bottom).

## Format
| Date | Context | What went wrong | Root cause | Corrective action (durable) |
|------|---------|-----------------|------------|------------------------------|
| (example) YYYY-MM-DD | task / area | one-line description | why it happened | the rule to follow next time |

## Log
| Date | Context | What went wrong | Root cause | Corrective action (durable) |
|------|---------|-----------------|------------|------------------------------|
| 2026-07-13 | Workflow init | (none yet — file created during initialization) | — | When the user gives project context, record any early mistakes here. |
| 2026-07-13 | Dataset organization | `inspect_data.py` crashed printing Thai Gmail filenames on the Windows cp1252 console. | stdout used the OEM code page; non-ASCII paths could not be encoded. | Reconfigure `sys.stdout/stderr` to UTF-8 (`errors="replace"`) at the top of every CLI script. |
| 2026-07-13 | Dataset discovery | Public datasets not found initially. | `_locate_named_child` and `_score_candidate` tested the folder name for exact membership in the hint tuple (`name in tuple`), so `sroie_receipts` never matched hint `sroie`. | Match by substring (`any(hint in name for hint in hints)`). |
| 2026-07-13 | Audit performance | Inventory over 129k files / 34 GB was far too slow single-threaded and even threaded when PIL `Image.load()` fully decoded large CORU receipt images. | Full pixel decode of ~31 GB of images dominated runtime. | Use header + `Image.verify()` (no full decode) for validation; thread the file work (hashing releases the GIL). Document the validation level. |
| 2026-07-13 | Near-duplicate scan | The scan hung for minutes inside one dimension bucket of ~9k same-size FATURA images (O(n²) pairwise). | Bucketing by (w,h) created a giant bucket. | Cap per-bucket pairwise work (truncate + log) and cap total images hashed; write core metadata before the optional near-dup step so a slow scan can never lose the inventory. |
| 2026-07-13 | FATURA pairing | First audit reported 20,000 "unmatched annotations" — a false alarm. | Stem-exact matching missed COCO/HF annotation files named `<image>_coco_test.json` / `<image>_hugg_test.json`. | For multi-format datasets, match an annotation to an image when the image stem is the annotation stem or a `_`-delimited prefix of it. |
| 2026-07-13 | Rotation materialization | A literal full-corpus, full-angle run would exceed the machine's safe disk budget. | The 22,086 usable public pages imply 416,028 rotations and an empirical estimate of 219.08 GiB new space, versus 16.12 GiB free at the final gate and a 10 GiB reserve. | Run an empirical smoke profile first, enforce a free-space reserve, and label any deterministic corpus cap explicitly as a bounded full-angle run. |
| 2026-07-13 | Unsupervised rotation model | Four K-Means clusters did not recover the four desired quadrants reliably; held-out accuracy stayed near 38% and exact-angle median error was 90 degrees. | Handcrafted orientation features and document symmetry can group axis-equivalent or content-similar pages rather than the predefined angle labels. | Report raw clustering and mapped metrics separately, preserve boundary/confidence evidence, and do not promote the baseline without a method change and professor confirmation. |
| 2026-07-13 | Stage transition | Organization-only dependency comments, ignore rules, and forbidden-output checks became stale when modeling artifacts were introduced. | Lifecycle controls encoded the old stage as permanent policy instead of distinguishing approved rotation outputs from still-forbidden OCR/neural outputs. | When a project enters a new stage, audit requirements, ignore rules, verifiers, README, memory, and tests together before calling the stage complete. |
| 2026-07-13 | Privacy review | Real private filenames entered otherwise synthetic test fixtures, while the verifier scanned only generated artifact roots. | Test data was copied from live private examples and privacy coverage was directory-limited. | Use invented fixture names and scan committable source, tests, docs, config, metadata, reports, and models against the private-name inventory. |
| 2026-07-13 | Derived artifact reuse | Existing page and rotation PNGs were reused after settings changed and then relabeled with new manifest hashes. | Reuse validated image readability but did not bind the file to its source hash and configuration. | Embed source/configuration provenance in materialized PNGs and regenerate atomically whenever provenance does not match. |
| 2026-07-15 | Windows GPU runtimes | Paddle GPU and CUDA PyTorch failed when imported into one environment/process. | Their Windows wheels bundled incompatible cuDNN DLL versions. | Keep Paddle in the OCR process with CPU-only Torch and run CUDA Torch/Transformers in a separate layout environment and subprocess; probe both independently. |
| 2026-07-15 | OCR environment reproducibility | The optional K-Means display failed because unbounded `opencv-python-headless>=4.9` installed OpenCV 5 beside PaddleX's 4.10 build, removing `HOGDescriptor`. | A broad dependency constraint allowed an API-incompatible major version to shadow the expected binary. | Pin the verified OpenCV build and keep display-only computations free of plotting-heavy baseline imports. |
| 2026-07-15 | Multilingual test fixtures | A PowerShell here-string piped to Python converted Thai source text to question marks, initially producing a false routing failure. | Windows pipeline encoding lost non-ASCII characters before Python rendered the fixture. | Generate Unicode fixtures from a UTF-8 source file or direct Unicode-safe argument, visually inspect the fixture, and assert source-script presence before interpreting OCR results. |
| 2026-07-15 | Evaluation references | A CORU page without source OCR tokens produced a huge, meaningless CER when its insertion count was divided by one. | Missing ground truth was treated as an empty reference instead of an unavailable metric. | Exclude unavailable OCR references from CER/WER denominators, report reference coverage, and leave dataset metrics null when no valid reference exists. |
| 2026-07-15 | Public-safe manifests | Redacting private paths alone left private source SHA-256 and duplicate-group fingerprints in public-facing manifest rows. | The redaction policy treated only readable paths as identifiers. | Blank private paths, source hashes, and grouping fingerprints in committable manifests; retain them only in ignored operational metadata and verify all fields. |
| 2026-07-15 | Integration evidence | A hand-authored integration report could pass verification without proving which runner, sources, models, fixtures, or outputs produced it. | The verifier trusted reported booleans instead of binding evidence to executable artifacts. | Generate integration reports from a tracked runner; hash source/model/config/checkpoint/fixture/output artifacts and independently revalidate output schema and semantics. |
| 2026-07-15 | Rotated OCR smoke | The rotated model check allowed zero words and therefore passed an engine invocation that did not prove rotation recovery. | `require_words=False` weakened the assertion at exactly the path being verified. | Run the cardinal selector and require nonempty OCR, a nonzero correction, exact model identities, and known phrase fragments. |
| 2026-07-15 | Automatic Thai routing | A Thai PDF page misread as plausible Latin at confidence 0.716 never triggered the Thai recognizer; Thai script scoring could also exceed 1.0. | The retry threshold was below the calibrated confidence center and combining marks were counted only in the numerator. | Retry Thai below the 0.75 calibrated center, compare route scores, bound script ratios by a script-aware denominator, and regression-test both conditions. |
| 2026-07-17 | Final checkpoint selection | An early final-training attempt selected only on upright validation quality and had to be stopped and restarted before completion. | The selection objective did not represent the required arbitrary-angle behavior, so a high upright score could hide rotation fragility. | Define the checkpoint objective before the run and include a fixed robustness slice; the final protocol weights upright dev-select at 0.7 and the 37-degree slice at 0.3. |
| 2026-07-17 | Annotation normalization | Unsupported CORU records were written to the normalized output tree before their exclusion was decided, leaving thousands of orphan JSON files. | Materialization happened before the adapter's eligibility result was final. | Decide eligibility first, write only supported records, and compare the generated tree against the authoritative manifest before exact-path cleanup. |
| 2026-07-17 | Regression-test editing | A context-misaligned patch placed a fake OCR return value in the wrong fixture method, briefly making `FakeOCR.extract_page` return `None`. | Similar nearby test helpers made a broad patch context match the wrong method. | Use the smallest unique patch context around fixture methods and immediately run the whole affected test module after editing shared fakes. |
| 2026-07-17 | Reproduction commands | Final-training examples omitted the explicit final checkpoint path, so the CLI default would write a nested build while later commands used the parent checkpoint. | Documentation assumed the intended artifact path matched a profile-dependent CLI default. | Make artifact-producing commands explicit about the exact checkpoint path and verify adjacent train/calibrate/infer commands resolve to the same directory. |
| 2026-07-17 | Calibrated inference | An explicitly configured but missing calibration file silently activated default thresholds. | The development fallback for an omitted calibration was also applied to a broken final configuration. | Allow defaults only when calibration is intentionally unspecified; fail closed when a supplied path is missing or hash-mismatched. |
| 2026-07-17 | Private CLI boundary | The general CLI trusted the caller to add `--private-output`, allowing a configured Gmail input to target a public directory when the flag was omitted. | Privacy mode was treated as caller intent instead of being derived from the input path. | Resolve every input against configured private roots before opening it, require private mode automatically, and independently constrain the output root. |
| 2026-07-17 | Integration provenance | Hash-bound integration omitted the subprocess client, worker, learned inference, and model implementation on the real call path. | Provenance stopped at the top-level pipeline wrapper instead of following the transitive execution path. | Enumerate and regression-test every executable source across process boundaries in both the report generator and verifier. |
| 2026-07-17 | Model-example reuse | Existing aligned examples were reused after checking only build/profile/stream identifiers. | Those identifiers did not prove freshly derived annotation content still matched the serialized example. | Compare every reusable example with the freshly derived expected payload; require an explicit `--force` rewrite after reviewed source drift. |

## Recurring watch-items (promote here if a lesson repeats)
- When a CLI must print filesystem paths on Windows, force UTF-8 stdout up front.
- When a Python pipeline processes a large image corpus, never use `Image.load()` for validation; use `Image.open().verify()`.
- Per-bucket or per-group duplicate comparisons must be size-capped to stay bounded.
- Treat a four-cluster result as a hypothesis until mapped held-out metrics show that clusters correspond to the required four zones.
