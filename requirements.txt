# Runtime dependencies shared by dataset preparation, the preserved classical
# rotation baseline, and lightweight information-extraction utilities. Heavy
# Shared OCR/layout packages are in requirements-ie.txt; framework-specific
# packages are pinned in requirements-ocr.txt and requirements-layout.txt for
# the separate Python 3.10 environments on D:.
numpy>=1.26
pandas>=2.0
Pillow>=10.0
PyMuPDF>=1.23
imagehash>=4.3
PyYAML>=6.0
tqdm>=4.65
opencv-python-headless==4.10.0.84
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8
pytest>=7.0
jsonschema>=4.22
python-dateutil>=2.9
